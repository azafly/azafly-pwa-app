# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

# name: Deploy to Firebase Hosting on PR
# 'on': pull_request
# jobs:
#   build_and_preview:
#     if: '${{ github.event.pull_request.head.repo.full_name == github.repository }}'
#     runs-on: ubuntu-latest
#     env: 
#       REACT_APP_CLIENT_API_TOKEN:  '${{ secrets.REACT_APP_CLIENT_API_TOKEN }}'
#       REACT_APP_FIREBASE_API_KEY:  '${{ secrets.REACT_APP_FIREBASE_API_KEY }}'
#       CI: false
#     steps:
#       - uses: actions/checkout@v2
#       - run: 'sudo npm install -g firebase-tools && yarn && yarn deploy:staging'
#       - uses: FirebaseExtended/action-hosting-deploy@v0
#         with:
#           repoToken: '${{ secrets.REPO_TOKEN }}'
#           firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_PICK_SAFE }}'
#           projectId: pick-safe
